

typedef struct row
{
	int array[10];
} Row;

Row parent;
Row child;

int checkValues(Row row){
	if (parent->array[0]==0)
	{
		*child->array[0]=0;
	}

}

int main ()
{
 parent->array={0,0,0,0,1,0,0,0}
 checkValues(parent);
 prinf(child->array)
}
